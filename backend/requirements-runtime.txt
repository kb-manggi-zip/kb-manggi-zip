# 서버 런타임 전용 의존성 (배포용 — Render 등). 수집 스크립트(pandas/PublicDataReader)·
# 개발 도구(pytest/ruff/pre-commit)는 제외해 빌드를 가볍게 한다.
# 로컬 개발/테스트는 requirements.txt 사용.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
pyyaml==6.0.2
sqlalchemy==2.0.36
httpx==0.28.1
sse-starlette==2.1.3
anthropic==0.42.0        # 키(ANTHROPIC_API_KEY) 없으면 호출 안 함 → 결정론 폴백
langgraph                # 분석 그래프
langfuse                 # 트레이싱 (LANGFUSE_* 키 없으면 no-op)
# psycopg[binary]        # Postgres로 갈 때만 주석 해제 (기본 SQLite는 표준 라이브러리)
