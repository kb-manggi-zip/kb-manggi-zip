# 하루시뮬(SC-07) 씬 이미지 생성 계획

> 초안 — 팀 확인 후 실행(비용이 한 번에 나가는 배치라 사전 합의 필요)

## 뭘 만드는가

SC-07 "하루시뮬"은 지금 화면 틀만 있고 5개 시간대 씬을 Unsplash **스톡사진**으로 채운 가라 상태(갈래 3개끼리도 같은 사진 재사용, 지역 커스텀은 82개 동 중 3개뿐). 이걸 **동네마다 실제로 다른 AI 생성 이미지**로 바꾸는 계획.

## 핵심 구조 — 고정 3씬(아침·점심·저녁), 내용만 동마다 다르게

씬 개수는 **고정 3개**(가변 아님). 이유: 개수가 가변이면 "태그 많은 동 = 씬 많음 = 왠지 더 매력적으로 보임"이라는 착시가 생김(실제 순위는 스코어링이 이미 매기는데, 씬 개수가 또 다른 신호처럼 보이면 왜곡). 고정 3개면 이 착시가 없다.

- **아침 = 통근** (항상): 초역세권 태그 있으면 역 장면, 없으면 골목길
- **점심·저녁 = 상권 태그 중 2개**: 그 동이 가진 태그(음식점·카페 밀집/공원 인접/마트·편의점 밀집/학원가/**여가시설 밀집**) 중 **이 사람에게 맞는 것부터** 채운다.

**"이 사람에게 맞는 것"의 기준 — 사람 개인화는 여기서 들어간다.** 페르소나의 소비신호(`persona.consumptionSignals`, 이미 계산돼있고 `RegionList.tsx`의 leadSignal이 같은 데이터를 씀)를 읽어서, **근거가 확실한 신호만** 매칭한다 — 억지 연결은 안 함:

| 신호 | 매칭 | 근거 |
|---|---|---|
| 카페 많이 쓰는 편 | 음식점·카페 밀집 우선 | 직접 대응 |
| 식비 많이 쓰는 편 | 음식점·카페 밀집 우선 | SBIZ `dining_cafe` 필드 자체가 음식점+카페 합산이라 같은 데이터 |
| 쇼핑 많이 쓰는 편 | 마트·편의점 밀집 우선 | 제일 가까운 물리적 상권 |
| 여가 많이 쓰는 편 | **여가시설 밀집 우선** | 직접 대응 (기존엔 이미지 카테고리에서 빠져서 "공원"으로 억지 매칭했었음 — 원복) |
| 배달 많이 쓰는 편 | **매칭 없음** | 배달은 동네 밀집도와 무관(어디서든 시켜먹음) — 억지로 붙이지 않음 |

매칭되는 신호가 있으면 그 태그를 먼저 배치, 남은 자리는 그 동의 나머지 보유 태그로 채운다. **매칭 자체가 없으면(배달 신호이거나 신호가 없을 때)** 기본 순서(음식점·카페>공원>마트>여가>학원가, 잠정)로 폴백.

→ 정리: **씬 개수는 고정 3개**(착시 방지) + **그 안의 콘텐츠는 동 기준**(태그) + **어떤 걸 먼저 보여줄지는 사람 기준**(소비신호) — 세 가지가 동시에 성립한다.

## 여가시설 씬은 뭘 보여줄지 — 미결정, 두 방향

"여가시설 밀집"은 SBIZ 분류상 헬스장·영화관·노래방·오락실·관광명소·스포츠시설을 다 뭉뚱그린 카테고리라(`classify()`가 "여가"/"스포츠"/"관광"/"오락"을 전부 하나로 합침), 음식점·카페 밀집·마트처럼 "이거다" 싶은 단일 이미지가 없다. 지금 데이터로는 그 동의 여가시설이 헬스장 위주인지 노래방 위주인지 구분이 안 된다(숫자만 있고 세부 업종 미집계 — "공원" 키워드검색 부정확 문제와 같은 종류의 한계).

- **방향 A(간단)**: 세부 구분 없이 중립적인 하나의 "여가를 즐기는 거리" 이미지로 통일. 지금 데이터로 바로 가능, 대신 부정확함을 그대로 안고 감.
- **방향 B(세부 업종 재수집)**: `refresh_regions.py`를 고쳐서 SBIZ 원본 응답의 세부 업종(스포츠/오락/관광 등)까지 저장 → 그 동에서 우세한 유형에 맞는 이미지로 분기. API 재호출 + 코드 작업 필요, 그리고 "헬스장 하나 그린다고 그 동네 느낌이 되진 않는다"는 근본적 한계는 여전함.

**업데이트(논의 후)**: 방향 B보다 §랜드마크(아래) 쪽이 더 나을 수 있다는 의견 — 세부 업종보다 실제 장소 이름(카카오 `place_name`)이 여가시설뿐 아니라 전 카테고리에 범용적으로 쓰이는 개선이라서. 방향 B는 보류, 랜드마크 활용 여부를 먼저 결정하는 쪽으로.

**아직 고민 중(결론 아님)**: A(중립 이미지)로 일단 채울지, 아니면 §랜드마크 여부부터 먼저 정하고 그 결론에 따라 여가시설도 같이 처리할지 — 방향 B(세부 업종 재수집)는 우선순위에서 밀려난 상태고, 지금은 A vs 랜드마크 활용 중 뭘 먼저 결정할지가 열린 질문.

## "동마다 실제로 달라지나?" — 카테고리는 5개뿐인데

카테고리(장면 종류)는 통근+상권 5종(카페/공원/마트/여가/학원가)뿐이지만, **이미지는 카테고리별로 82개 동이 나눠 쓰는 게 아니라 동마다 따로 생성**한다. 같은 "음식점·카페 밀집" 카테고리라도:
- 동마다 별도 이미지 생성(파일 공유 안 함)
- 프롬프트에 그 동의 실제 수치(카페·음식점 356곳 vs 300곳 등)를 넣어 "북적임 정도"를 다르게 표현 가능

다만 한계는 인정한다 — 유한한 카테고리로 무한한 동네를 완전히 다르게 그릴 순 없고, 예산대가 비슷해 실제로 특징이 겹치는 동네는 이미지도 비슷하게 나올 수 있다. **이건 왜곡이 아니라 정직한 반영**(비슷한 동네를 억지로 다르게 그리면 그게 오히려 임의 작문). "이 동네가 맞다"는 확신은 이미지가 아니라 동 이름·실측 통근분·실거래가 같은 텍스트가 담당 — 이미지는 분위기만.

## 갈래·가구유형은 이미지에 안 나눔

- **갈래(이사/매매/갱신)**: 물리적 거리는 계약 형태로 안 바뀜 — 캡션(`caption1`/`caption2`)만 갈래별, 이미지는 공용.
- **가구유형별 이미지 자체는 안 나눔**(비용 3~4배) — 대신 위 §핵심 구조의 "점심·저녁 우선순위" 로직으로 사람 개인화를 반영 + 기존 텍스트 내레이션이 나머지를 담당.

## 스코프·비용

**82개 동 × 3씬(고정) = 246장.** 표준 요율(장당 $0.039) 기준 약 **1.3만원**. 1회성 비용.

## 이미지엔 랜드마크 이름 안 넣음

"합정역"이라고 프롬프트에 넣어도 실제 합정역은 안 그려짐 — 이미지는 계속 일반화된 분위기만. **캡션(텍스트)에 실제 역·공원 이름을 넣을지는 미정**(지금 3개 동 커스텀 씬은 이미 그렇게 함). 이름 데이터(카카오 API의 `place_name`)는 사용 여부와 무관하게 지금 저장해두는 걸 추천(같은 API 호출 재사용, 나중에 다시 부르는 것보다 쌈).

## 프롬프트 뼈대

```
Photorealistic photo, {카테고리별 조명}, {카테고리별 장면 + 그 동 실제 수치로 강도 조절},
Seoul neighborhood street. A person seen from behind, {카테고리별 활동}, no visible face,
neutral. No text/logos/real signage.
```

## 팀 확인 필요

- [ ] 고정 3씬(아침=통근, 점심/저녁=상권 태그 2개) 구조 동의?
- [ ] 점심·저녁 우선순위를 소비신호(leadSignal)로 정하는 것 동의? 매칭 없을 때 기본순서(카페>공원>마트>여가>학원가, 잠정) 동의?
- [ ] 여가시설 씬 표현 방식 — 방향 A(중립 이미지 하나)로 시작, 방향 B(세부 업종)는 보류하고 랜드마크 활용으로 대체 검토 — 동의?
- [x] ~~"카페거리" 태그명이 실제 데이터(음식점 포함)와 안 맞는 문제~~ — "음식점·카페 밀집"으로 수정 완료(PR #89)
- [ ] 가구유형별 이미지 안 나누는 것 동의?
- [ ] Gemini vs Stable Diffusion
- [ ] 사람 뒷모습 포함 여부
- [ ] 랜드마크 이름 캡션에 쓸지 (데이터는 미리 저장 추천)
- [ ] 프론트 구현 위치(DayPlayer.tsx 리팩터 필요, 별도 작업량)
